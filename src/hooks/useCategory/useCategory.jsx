// import React from 'react';
// import useMenu from '../useMenu/useMenu';
// import MenuCard from '../../pages/Home/Shared/MenuCard/MenuCard';

// const useCategory = ({categoryName}) => {
//     const [menu] = useMenu()
//     const items = menu.filter(menuItem=>menuItem.category===categoryName)
//     return (
//         < div className = "grid grid-cols-1 md:grid-cols-2 gap-5 " >
//         {
//         items.map(item => <MenuCard key={item._id} item={item}></MenuCard>)
//                 }
//             </div >)
// };

// export default useCategory;